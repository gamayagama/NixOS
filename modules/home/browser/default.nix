{
  programs = {
    # May be replaced by librewolf later, but works better out of the box
    firefox = {
      enable = true;
    };
    # librewolf = {
    #   enable = true;
    #   settings = {};
    # };
  };
}
